#cloud-config
users:
  - name: homehome
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: [sudo, users, admin]
    shell: /bin/bash
    lock_passwd: true
    plain_text_passwd: homehome
    ssh_pwauth: false
    ssh_authorized_keys:
      - $ssh_public_key

runcmd:
- [ sed, -i, s/REGDOMAIN=/REGDOMAIN=FR/g, /etc/default/crda ]
- [ netplan, apply ]
